[package]
name = "sasktran2-sys"
version = "0.1.0"
edition = "2024"
build = "build.rs"
links = "sasktran2"

[features]
default = []
build-bindings = ["dep:bindgen"]

[build-dependencies]
cmake = "*"
bindgen = { version = "*", optional = true }

[dependencies]
ndarray = "*"
anyhow = "*"
